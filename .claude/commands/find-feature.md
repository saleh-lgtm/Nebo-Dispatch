---
description: Find all files related to a feature area
---
Search the codebase for all files related to "$ARGUMENTS" across: src/app/ (pages), src/lib/ (actions), src/components/ (UI), src/types/ (types), prisma/schema.prisma (models), src/app/api/ (routes), src/lib/schemas.ts (validation). List every file found, grouped by category.
